# GK-M1 Human Validation Record

Validation outcome recorded from the human operator's review of the GK-M1 validation pack.

## Result

**All seven proposed annotations: ACCEPTED**

Validated at: `2026-08-26T04:09:45Z`

### 2r — Image ID 1006078, 1428 × 2000

1. `VM.f2r.root_pivot` — **accept**
   - pixel: `(815, 1778)`
   - normalized: `(0.570728, 0.889000)`
   - operational landmark: center of junction where lower stem enters the red root structure

2. `VM.f2r.lower_stem_axis` — **accept**
   - segment: `(814,1725) -> (808,1260)`
   - operational landmark: lower central stem axis, before upper branching

3. `VM.f2r.root_region` — **accept**
   - bbox: `(610,1710) -> (1020,1920)`
   - operational region: coarse red root structure

### 2v — Image ID 1006079, 1460 × 2000

4. `VM.f2v.central_y` — **accept**
   - pixel: `(855,1075)`
   - normalized: `(0.585616, 0.537500)`
   - operational landmark: central bifurcation point in the Y/stub structure

5. `VM.f2v.upper_stem_axis` — **accept**
   - segment: `(900,1500) -> (862,1120)`
   - operational landmark: upper long-stem axis approaching the Y/stub

### 3r — Image ID 1006080, 1428 × 2000

6. `VM.f3r.root_crown` — **accept**
   - pixel: `(836,1570)`
   - normalized: `(0.585434,0.785000)`
   - operational landmark: crown of the root fan where the stem enters the root region

7. `VM.f3r.root_region` — **accept**
   - bbox: `(520,1545) -> (1170,1940)`
   - operational region: coarse visible root fan

## Validation scope

This human validation confirms only that each annotation adequately marks the selected visual primitive for operational use in the VOXIS instrument.

It does **not** confirm:

- a cross-folio correspondence;
- a geometric alignment;
- a recurring manuscript grammar;
- a procedural function;
- manuscript intent or meaning.

Those require separate recorded Search Sessions, measurements, comparisons, and—where applicable—null/control results.
