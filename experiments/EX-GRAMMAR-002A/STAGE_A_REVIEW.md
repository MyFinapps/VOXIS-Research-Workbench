# EX-GRAMMAR-002A — Stage A Human Review

**Proposal SHA-256:** `d483018f3c196ad7945e9fdcc4bc35e89bb13b428ab35895d6bb2d1e06a84a13`  
**Pairwise measurements run:** **NO**

Review only annotation adequacy and eligibility. For each item choose `ACCEPT`, `ADJUST`, or `REJECT`.

## Proposed STEM actors

- `S1` — corrected upper/middle main-stem axis; proposed replacement for prior offset `VM.f2v.upper_stem_axis`.
- `S2` — lower main-stem section.
- `S3` — flower-stalk axis above the large green field.

## Proposed Y targets

- `Y1` — corrected central Y-like junction; proposed replacement for prior offset `VM.f2v.central_y`.
- `Y2` — side-leaf/main-stem fork; included provisionally for eligibility review.

## Recorded exclusions

- `X1` — flower/calyx multi-lobed transition; not a clear Y.
- `X2` — petal-outline V; not a botanical branch junction.
- `X3` — short side leaf/spur; not an elongated stem actor.
- `X4` — faint background/bleed-through region; excluded as uncertain.

## Binding defect discovered

The prior GK-M1 `VM.f2v.central_y` and `VM.f2v.upper_stem_axis` annotations failed this closer Stage A annotation audit. EX-GRAMMAR-001 R1 is retained historically but invalidated for evidentiary use.

The local review pack contains source-raster overlays only; it contains no pairwise guide lines, distances, angular deviations, rankings, or tolerance outcomes.
